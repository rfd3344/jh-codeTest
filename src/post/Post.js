import { Container, Box, Typography } from '@mui/material';

export const Post = () => {
  return (
    <Container
      maxWidth="lg"
      component={Box}
      mt={2}
      py={1}
      bgcolor="grey.light"
      borderRadius={1}
    >
      1111
    </Container>
  );
};
