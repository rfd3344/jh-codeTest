import { Container, Box, Paper } from '@mui/material';
import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({}));

export const SignupForm = () => {
  const classes = useStyles();

  return <Box>SignupForm</Box>;
};
