import { Container, Box, Paper } from '@mui/material';
import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({}));

export const LoginPanel = () => {
  const classes = useStyles();

  return <Box>LoginPanel</Box>;
};
